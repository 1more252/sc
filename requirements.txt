# Scarlett — зависимости
# Запусти install.bat для автоустановки

# ── Ядро ──────────────────────────────────────────────────────────────────────
speechrecognition>=3.10.0
pyaudio>=0.2.11
pyttsx3>=2.90
requests>=2.31.0
psutil>=5.9.0
pywin32>=306
comtypes>=1.2.0
pycaw>=20181226
pillow>=10.0.0
pygame>=2.5.0
pystray>=0.19.4
edge-tts>=6.1.9

# ── АПГРЕЙД 1: Долгосрочная векторная память ──────────────────────────────────
# ChromaDB — локальная векторная БД, работает оффлайн, без регистрации
# Хранит воспоминания в ~/.scarlett_memory/
chromadb>=0.4.0
# Embeddings для семантического поиска (скачивает модель ~90 МБ один раз)
sentence-transformers>=2.2.0

# ── АПГРЕЙД 3: Системный контекст (активное окно) ─────────────────────────────
# pywin32 уже выше — win32gui входит в него

# ── АПГРЕЙД 4: LLM Tool Calling ───────────────────────────────────────────────
# Не требует новых пакетов — использует существующие Groq/Mistral API
# Нужен хотя бы один API ключ: groq_key или mistral_key в настройках
